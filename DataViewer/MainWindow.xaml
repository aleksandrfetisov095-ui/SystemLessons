<Window x:Class="DataViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataViewer"
        mc:Ignorable="d"
        Title="Просмотр данных" Height="600" Width="900">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        
        <TextBlock Text=" UDP Монитор сообщений" 
                   FontSize="20" 
                   FontWeight="Bold" 
                   Margin="0,0,0,15"/>

        
        <Border Grid.Row="1" 
                Background="White" 
                Padding="10" 
                CornerRadius="5"
                Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal">
                <Button Content=" Загрузить с сервера" 
                        Click="LoadFromServer_Click"  
                    Padding="15,8" 
                        Margin="0,0,10,0"
                        Background="Blue" 
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"/>


                <Button Content=" Экспорт в CSV" 
                        Click="ExportToCsv_Click"
                        Padding="15,8" 
                        Margin="0,0,10,0"
                        Background="Green" 
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"/>
                <Button Content=" Отправить тестовые данные" 
                        Click="SendTestData_Click"
                        Padding="15,8" 
                        Margin="0,0,10,0"
                        Background="Orange" 
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"/>

                    
                    <Button Content=" Очистить на сервере" 
                        Click="ClearServerData_Click"  
                        Padding="15,8"
                        Background="Red" 
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"/>
            </StackPanel>
        </Border>

       
        <DataGrid Grid.Row="2" 
                  x:Name="MessagesDataGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  AlternatingRowBackground="White"
                  BorderBrush="White"
                  BorderThickness="1"
                  SelectionMode="Single">
            

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" 
                        Binding="{Binding Id}" 
                        Width="50"/>
                <DataGridTextColumn Header="Сообщение" 
                        Binding="{Binding Content}" 
                        Width="*"/>
                <DataGridTextColumn Header="Время получения" 
                        Binding="{Binding ReceivedTime}" 
                        Width="180"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="3" 
                Background="White" 
                Padding="10" 
                CornerRadius="5" 
                Margin="0,15,0,0">
            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Center">
                <TextBlock x:Name="StatusText" 
                           Text="Готов к работе" 
                           Foreground="White"
                           VerticalAlignment="Center"/>

                <TextBlock x:Name="CounterText" 
                           Text="Всего записей: 0" 
                           Foreground="#2c3e50"
                           FontWeight="Bold"
                           Margin="20,0,0,0"/>

                <ProgressBar x:Name="LoadingProgressBar" 
                             Width="100" 
                             Height="15"
                             Margin="20,0,0,0"
                             IsIndeterminate="True"
                             Visibility="Collapsed"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
